<Window x:Class="DicomTagChecker.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="350" Width="600" WindowStartupLocation="CenterScreen" FontFamily="Meiryo">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>
        <ContentControl prism:RegionManager.RegionName="ContentRegion" />
        <TextBlock Text="監視対象フォルダ：" Height="25" Width="105" Margin="20,20,470,20"/>
        <TextBox x:Name="FolderPathtextBox" Text="{Binding FolderPath}" IsReadOnly="True"
                 Height="25" Width="380" TextWrapping="NoWrap" Margin="60,20,0,20"/>
        <Button Content="..." Height="20" Width="25" Margin="490,22,10,16"/>

        <TextBlock Grid.Row="1" Text="ログ" Height="25" Width="20" Margin="0,0,490,235"/>
        <ListBox x:Name="LogListBox" Grid.Row="1" Height="160" Width="510" Margin="0,5,0,60"/>
        <Button x:Name="StartButton" Content="取込開始" Grid.Row="1" Height="30" Width="60" Margin="380,180,70,35"/>
        <Button x:Name="CancelButton" Content="キャンセル" Grid.Row="1" Height="30" Width="60" Margin="450,180,0,35"/>

        <StatusBar Grid.Row="1" VerticalAlignment="Bottom">
            <Label Name="StatusBarLabel" Content="test"/>
        </StatusBar>
    </Grid>
</Window>
